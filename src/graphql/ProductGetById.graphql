query ProductGetById($productId: ID) {
	product(id: $productId) {
		data {
			attributes {
				name
				description
				price
				images {
					data {
						attributes {
							url
							alternativeText
							width
							height
						}
					}
				}
			}
		}
	}
}
